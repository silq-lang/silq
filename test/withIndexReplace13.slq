// args: --run

def myH(x:𝔹)mfree{
	x:=H(x);
	return x;
}

def main(){
	i:=measure(H(0));
	xs:=(H(0),H(0));
	with t:=myH(xs[i]){
		t⊕=1;
	}
	with t:=myH(xs[1-i]){
		t⊕=1;
	}
	xs[0]:=H(xs[0]);
	xs[1]:=H(xs[1]);
	assert(measure(xs=(1,1)));
	return xs;
}
