// args: --run

def main(){
	x₀:=[(0:!𝔹,0:𝔹),vector(2,0:𝔹)]:(𝔹^2)[];
	x₁:=[vector(2,0:𝔹),(0:!𝔹,0:𝔹)]:(𝔹^2)[];
	y₀:=[array(2,0:𝔹),(0:!𝔹,0:𝔹)]:𝔹[][];
	y₁:=[(0:!𝔹,0:𝔹),array(2,0:𝔹)]:𝔹[][];
	z₀:=[vector(2,0:𝔹),array(2,0:𝔹)]:𝔹[][];
	z₁:=[array(2,0:𝔹),vector(2,0:𝔹)]:𝔹[][];

	w₀:=((0,0):!𝔹×!ℤ):!ℤ[];
	return (x₀,x₁,y₀,y₁,z₀,z₁,w₀);
}
