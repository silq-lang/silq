
def main(){
	x:=2 coerce ℤstar[5];
	y:=1:ℤmod[5];
	(c,d):=(H(0),H(0));
	if c{
		z := 3 coerce !ℤstar[5];
	}else{
		z := 2 coerce !ℤstar[5];
	}
	if d{
		x*=z;
		y*=z;
	}
	d:=H(d);
	x*=z;
	y*=z;
	return (c,d,x,y);
}
