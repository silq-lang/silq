def main(){
    x₁ := Gauss(0,1);
    x₂ := Gauss(1,1);
    x₃ := Gauss(x₁,x₂);
    return (x₁,x₂,x₃); // expected: ([x₂=0]·δ[-x₁+x₃]·⅟2+[x₂≠0]·e^((-x₁²·⅟2+-x₃²·⅟2+x₁·x₃)·⅟x₂)·⅟2^(3·⅟2)·⅟√x̅₂̅·⅟√π̅)·[-x₂≤0]·e^(-x₁²·⅟2+-x₂²·⅟2+-⅟2+x₂)·⅟π
    // TODO: this could be simplified better
}