def main(){
    x₁ := Gauss(0,1);
    x₂ := Gauss(1,1);
    x₃ := Gauss(x₁,x₂);
    return (x₁,x₂,x₃); // expected: e^((-x₃²·⅟2+x₁·x₃)·⅟x₂+(-⅟2+-⅟2·⅟x₂)·x₁²+-x₂²·⅟2+-⅟2+x₂)·⅟2^(3·⅟2)·⅟π^(3·⅟2)·⅟√x̅₂̅
}