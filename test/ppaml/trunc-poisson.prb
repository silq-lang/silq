
def main(){
    /*bound := 3;
    lambda := 4;
    L := SampleFrom("(L)=>δ[L-e^(-lambda)]"); // TODO: add constant e
    p := 1;
    r := -1;
    for k in [0..bound){
	if r==-1{
	    p = p*Uniform(0,1);
	    if p <= L{ r = k; }
	}
    }
    return r;*/
    lambda := 1;
    L := SampleFrom("(L)=>δ[L-e^(-lambda)]"); // TODO: add constant e
    u1 := Uniform(0,1);
    assert(u1>L);
    u2 := u1*Uniform(0,1);
    assert(u2>L);
    u3 := u2*Uniform(0,1);
    assert(u3>L);
    u4 := u3*Uniform(0,1);
    assert(u4>L);
    u5 := u4*Uniform(0,1);
    assert(u5>L);
    u6 := u5*Uniform(0,1);
    assert(u6<=L);
}
