// args: --run

def myH(x:𝔹)mfree{
	x:=H(x);
	return x;
}

def main(){
	xs:=(H(0),0);
	with t:=myH(xs[0]){
		with u:=myH(xs[1]){
			u⊕=t;
		}
		t⊕=1;
	}
	xs[0]:=H(xs[0]);
	assert(measure(xs=(1,0)));
	return xs;
}
