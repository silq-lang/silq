
def main(){
    x := Uniform(0,1);
    y := Uniform(0,1);
    z := x + y;
    return z; // expected: (([-2+z≤0]·[-z+1≠0]+[-z+1=0])·[-z+1≤0]+[-1+z≤0]·[-z+1≠0]·[-z≤0])·(-[-z+1≠0]·[-z+1≤0]·z+2·[-z+1≠0]·[-z+1≤0]+[-1+z≤0]·z)

    // TODO: the expression is correct, but hard to understand.
    // Make it look a little bit closer to max(0,min(1,z)-max(0,z-1))
}
