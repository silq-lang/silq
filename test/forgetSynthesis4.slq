def main() {
	x:=1:𝔹;
	y:=0:𝔹;
	if x{
		y:=H(y);
	}
	return nonQfree(x, y);
}

def nonQfree(const x:B,y:B){
	t := dup(x);
	t := H(t);
	t := I(t); // error
	if t{
		y := X(y);
	}
	return (x, y);
}
