def max(a,b){
    r := if a > b { a } else { b };
    return r;
}

def main(){
    x := gauss(0,1);
    y := gauss(0,1);
    r := max(x,y);
    return r; // expected: (d/dx)⁻¹[e^(-x²)](r·⅟√2̅)·⅟e^(1/2·r²)·⅟π·√2̅
}
