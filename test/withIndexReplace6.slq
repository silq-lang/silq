// args: --run

def myH(x:𝔹)mfree{
	x:=H(x);
	return x;
}

def main(){
	xs:=((0,1),(H(1),0));
	with t:=myH(xs[1][0]){
		t⊕=1;
	}
	xs[1][0]:=H(xs[1][0]);
	assert(measure(xs=((0,1),(0,0))));
	return xs;
}
