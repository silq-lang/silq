
def main(){
    skill1 := Gauss(100,10);
    skill2 := Gauss(100,10);
    perf1 := Gauss(skill1,15);
    perf2 := Gauss(skill2,15);
    observe(perf1 > perf2);
    return (skill1,skill2); // expected: TODO: simplify better
}