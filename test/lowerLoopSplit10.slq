// args: --remove-loops --run

def foo(const a:𝔹){
	c:=0:𝔹;
	d:=0:𝔹;
	for i in [0..3){
		if a {
			c⊕=1;
			d:=H(d);
		}
		if c { d:=X(d); }
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a);
	return (a,r);
}
