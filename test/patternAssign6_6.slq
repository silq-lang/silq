// args: --run

def foo[n:!ℕ](x:uint[n]){
	x+=2;
	return x;
}

n:=5;
def main(){
	x:=5:uint[n];
	t:=foo[5](x);
	t·=3;
	foo[5](x):=t;
	t:=foo[5](x);
	t·=3;
	foo[5](x):=t;
	assert(measure(x=29));
	return x;
}

