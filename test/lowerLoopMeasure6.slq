// args: --remove-loops --run

def foo(const a:𝔹){
	c:=dup(a);
	d:=0:𝔹;
	for i in [0..3){
		while measure(H(0:𝔹)){
			c⊕=1;
			if c { d:=H(d); }
		}
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a);
	return (a,r);
}
