def main(){
    // x₁ := UniformInt(1,10);
    x₁ := Gauss(0,1);
    x₂ := Gauss(1,1);
    x₃ := x₁ + x₂;
    x₄ := x₃ + Bernoulli(1/2);
    x₅ := x₄ + Bernoulli(1/2);
    x₆ := x₅ + Bernoulli(1/2);
    x₇ := x₆ + Bernoulli(1/2);
    x₈ := x₇ + Bernoulli(1/2);
    x₉ := x₈ + Bernoulli(1/2);
    x₁₀ := x₉ + Bernoulli(1/2);
    x₁₁ := x₁₀ + Bernoulli(1/2);
    x₁₂ := x₁₁ + Bernoulli(1/2);
    x₁₃ := x₁₂ + Bernoulli(1/2);
    return x₁₃; // expected: (105·e^(-(-4+x₁₃)²·⅟4+-2+x₁₃·⅟2)·⅟1024+105·e^(-(-6+x₁₃)²·⅟4+-3+x₁₃·⅟2)·⅟1024+15·e^(-(-3+x₁₃)²·⅟4+-3·⅟2+x₁₃·⅟2)·⅟256+15·e^(-(-7+x₁₃)²·⅟4+-7·⅟2+x₁₃·⅟2)·⅟256+45·e^(-(-2+x₁₃)²·⅟4+-1+x₁₃·⅟2)·⅟2048+45·e^(-(-8+x₁₃)²·⅟4+-4+x₁₃·⅟2)·⅟2048+5·e^(-(-1+x₁₃)²·⅟4+-⅟2+x₁₃·⅟2)·⅟1024+5·e^(-(-9+x₁₃)²·⅟4+-9·⅟2+x₁₃·⅟2)·⅟1024+63·e^(-(-5+x₁₃)²·⅟4+-5·⅟2+x₁₃·⅟2)·⅟512+e^(-(-10+x₁₃)²·⅟4+-5+x₁₃·⅟2)·⅟2048+e^(-x₁₃²·⅟4+x₁₃·⅟2)·⅟2048)·⅟√π̅·⅟∜e̅
    // TODO: maye simplify better (e.g. with binomial coefficients)
}
