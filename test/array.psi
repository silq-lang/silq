def main(){
    a := readCSV("data/array/array.csv");
    //observe(a.length==3);
    k := array(7);
    k[0]=3;
    k[1]=flip(0.5);
    // k[2]=Uniform(0,k[UniformInt(0,1)]); // TODO!
    r := k[0]+k[1]+k[2]+k[3]+a[0]+a[1]+a[2];
    return r; // expected: 1/2·δ(10)[r]+1/2·δ(9)[r]
}
