// args: --run

def main(){
	let{ y := x; for i in 0..3 { y += i as uint[4]; } } in y := 3:uint[4];
	assert(measure(x)=(0:!uint[4]));
	let{ y := w; for i in 0..3 { y += i as !uint[4]; } } in y := 3:!uint[4];
	assert(w=(0:!uint[4]));
	return 0;
}
