// args: --run

def foo(const x:𝔹,y:!𝔹,z:!𝔹)wild{
	if measure(H(0)){
		if x{
			if y{
				return 1;
			}
			if y{
				return 1;
			}
		}else if z{
			return 1;
		}
	}else{
		if x{
			if y{
				return 1;
			}
			if y{
				return 1;
			}
		}else if z{
			return 1;
		}
	}
	assert(!y||!z);
	return 0;
}

def main(){
	for y in 0..2{
		for z in 0..2{
			for k in 0..10{
				x:=H(0);
				assert(measure(foo(x,y≠0,z≠0))||y=0||z=0);
				measure(x);
			}
		}
	}
}
