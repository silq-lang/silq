// args: --run

def foo(const x:!𝔹,const y:!𝔹)wild{
	if x{
		return 1;
	}
	if y{
		return 1;
	}
	assert(0);
}

def main(){
	for i in 0..2{
		for j in 0..2{
			if i≠0||j≠0{
				assert(measure(foo(i≠0,j≠0)));
			}
		}
	}
}
