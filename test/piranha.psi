// Simple example from Joost-Pieter Katoen's talk
// TODO: arrays / tuples

def main(){
    a := flip(1/2); // 1 -- piranha
    b := 1; // add piranha
    r := if flip(1/2) { a } else { b };
    observe(r);
    return a; // expected: 2/3·δ[-a+1]+δ[a]·⅟3
}
