// args: --run

def main(){
    x := H(0:𝔹);
    if measure(H(0:𝔹)) {
        y := H(0:𝔹);
		measure(x);
        return H(y);
    } else if measure(H(0:𝔹)) {
        z := H(0:𝔹);
		measure(x);
        return z;
    } else {
        return x;
    }
}
