def foo[m:!ℕ,n:!ℕ][σ:*^m,τ:*^n](xs: ∏ σ, ys: ∏ τ)qfree⇒(xs,ys);

def main(){
	x:=(():(𝔹^2)^0,((0,1,0),):(𝔹^3)^1):𝟙×(𝔹^3)^1;
	a:=():(𝔹^2)^0;
	b:=((0,1,0),):(𝔹^3)^1;
	return foo[0,1][(),(𝔹^3,)](a,b);
}
