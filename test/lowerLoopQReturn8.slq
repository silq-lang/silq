// args: --remove-loops --run

def f[n:!ℕ](const xs:𝔹^n)mfree{
	k:=0:!ℕ;
	q:=0:𝔹;
	for i in [0..n){
		if xs[i] { return (X(0:𝔹),i as uint[4]); }
	}
	k+=1;
	q:=H(q);
	return (q,k as uint[4]);
}
def main(){
	xs:=(H(0:𝔹),H(0:𝔹));
	r:=f(xs);
	return (xs,r);
}
