// args: --remove-loops --run

def two(x:𝔹)qfree{
	t:=dup(x);
	return (X(x),t);
}
def foo(const a:𝔹){
	c:=dup(a);
	d:=0:𝔹;
	for i in [0..3){
		if a {
			(c,t):=two(c);
			t⊕=1;
			if t { d:=H(d); }
			forget(t);
		}
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a);
	return (a,r);
}
