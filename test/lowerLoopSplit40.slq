// args: --remove-loops --run

def foo(const a:𝔹,const b:𝔹){
	c:=dup(a);
	e:=dup(b);
	d:=0:𝔹;
	for i in [0..3){
		with t:=dup(e) do {
			c⊕=t;
			d:=H(d);
		}
		if c { d:=X(d); }
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	b:=H(0:𝔹);
	r:=foo(a,b);
	return (a,b,r);
}
