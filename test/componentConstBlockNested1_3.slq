// args: --run

def main(){
	a:=vector(3,1:𝔹);
	a[0]:=H(a[0]);
	def foo(const z:𝔹,const t:𝔹)⇒if measure(H(0)) then if measure(H(0)) then z else t else t;
	if foo(a[0],a[2]){ a[1]:=X(a[1]); } /// error
	return a;
}
