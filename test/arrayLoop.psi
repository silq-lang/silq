def main(){
    a := array(11);
    r := 0;
    for i in [0..5){ a[i]=i+1; }
    for i in [0..5){ r+=a[i]; }
    x := flip(1/2);
    r += x;
    b := array(r-x);    
    return r; // expected: 1/2·δ(15)[r]+1/2·δ(16)[r]
}
