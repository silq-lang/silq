// args: --remove-loops --run

def foo(const a:𝔹,const b:𝔹){
	c:=dup(a);
	e:=dup(b);
	d:=0:𝔹;
	for i in [0..3){
		c⊕=1;
		m:=measure(H(0:𝔹));
		if m { e⊕=c; }
		if c&&e { d:=H(d); }
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	b:=H(0:𝔹);
	r:=foo(a,b);
	return (a,b,r);
}
