def p(a){
    x := Bernoulli(1/2);
    observe(x == a);
    return a;
}

def main(){
    y := Bernoulli(1/2);
    r := p(1);
    return r; // expected: TODO: δ[-r+1]
    // TODO: method calls are currently incorrectly implemented!
}