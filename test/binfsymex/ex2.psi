// Example 2.
def main(){
    c1 := flip(1/2);
    c2 := flip(1/2);
    observe(c1 || c2);
    return (c1,c2); // expected: (2·δ[-c1+1]·⅟3+2·δ[c1]·⅟3)·([c1=0]·[c2≠0]+[c1≠0])·(δ[-c2+1]·⅟2+δ[c2]·⅟2)
    // TODO: make output easier to interpret
}
