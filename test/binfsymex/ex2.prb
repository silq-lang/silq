// Example 2.
def main(){
    c1 := Bernoulli(1/2);
    c2 := Bernoulli(1/2);
    observe(c1 || c2);
    return (c1,c2); // expected: ((-4+4·[c1≠0])·(-[c2≠0]+1)+4)·(δ[-c1+1]·⅟6+δ[c1]·⅟6)·(δ[-c2+1]·⅟2+δ[c2]·⅟2)
    // TODO: make output easier to interpret
}
