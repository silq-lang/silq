def main(){
    x₁ := gauss(0,1);
    x₂ := gauss(1,1);
    x₃ := gauss(x₁,x₂);
    return (x₁,x₂,x₃); // expected: (1/2·[x₂=0]·δ(x₁)[x₃]·⅟π+[x₂≠0]·e^((-1/2·x₁²+-1/2·x₃²+x₁·x₃)·⅟x₂)·⅟2^(3/2)·⅟π^(3/2)·⅟√̅x̅₂)·[-x₂≤0]·e^(-1/2+-1/2·x₁²+-1/2·x₂²+x₂)
	//return (x₁,x₂);
}
