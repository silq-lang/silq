def main(){
    x₁ := gauss(0,1);
    x₂ := gauss(1,1);
    x₃ := gauss(x₁,x₂);
    return (x₁,x₂,x₃); // expected: ([x₂=0]·δ[-x₁+x₃]·⅟2·⅟π+[x₂≠0]·e^((-x₁²·⅟2+-x₃²·⅟2+x₁·x₃)·⅟x₂)·⅟2^(3/2)·⅟π^(3/2)·⅟√x̅₂̅)·[-x₂≤0]·e^(-1/2+-x₁²·⅟2+-x₂²·⅟2+x₂)
	//return (x₁,x₂); // TODO: fix crash
}
