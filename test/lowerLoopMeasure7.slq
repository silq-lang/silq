// args: --remove-loops --run

def foo(const a:𝔹){
	acc:=vector(3,0:𝔹);
	d:=0:𝔹;
	for i in [0..4){
		k:=measure(H(0:𝔹)) as !ℕ;
		acc[k]⊕=a;
		if acc[0] { d:=H(d); }
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a);
	return (a,r);
}
