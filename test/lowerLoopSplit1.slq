// args: --remove-loops --run

def foo(const a:𝔹,const b:𝔹){
	c:=dup(a);
	d:=dup(b);
	for i in 0..5{
		c⊕=1;
		if c { d:=H(d); }
		d:=X(d);
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a,0:𝔹);
	return (a,r);
}
