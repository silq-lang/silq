
def main(){
    x₁ := gauss(0,1);
    x₂ := gauss(0,1);
    x₃ := gauss(0,1);
    x₄ := gauss(0,1);
    x₅ := gauss(0,1);
    x₆ := gauss(0,1);
    x₇ := x₁+x₂;
    x₈ := x₃+x₄;
    x₉ := x₆+x₇;
    return x₁+2*x₂+3*x₃+4*x₄+5*x₅+6*x₆+7*x₇+8*x₈+9*x₉; // expected: 1/4·⅟e^(1/2256·r²)·⅟√1̅4̅1̅·⅟√π̅
}
