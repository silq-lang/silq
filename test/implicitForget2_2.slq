// args: --run

def foo(const x: 𝔹)qfree{
    y := dup(x);
    if true { // forgottenVarsOnEntry=[y]
        return 0: 𝔹;
    }
	assert(0);
}

def main(){
	x:=H(0:𝔹);
	y:=foo(x);
	forget(y=0);
	assert(measure(H(x))=0);
}
