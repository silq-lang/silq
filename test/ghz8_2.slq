// args: --run
def ghz(n:!ℕ){ // (not particularly useful)
	xs~[p]:=[H(0) for i in 0..n+1];
	if p{ for i in 0..n{ xs[i]:=Z(xs[i]); } }
	p:=measure(H(p));
	for i in 0..n{ xs[i]:=H(xs[i]); }
	if p&&xs[0] { phase(π); }
	return xs;
}

def main()⇒ghz(4);
