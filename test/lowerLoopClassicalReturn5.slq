// args: --remove-loops --run

def foo(const x:𝔹){
	y:=dup(x);
	z:=0:𝔹;
	k:=0:!ℕ;
	while k<3{
		y⊕=1;
		if y { z:=H(z); }
		if measure(H(0:𝔹)) { return (y,z); }
		k+=1;
	}
	forget(y=X(dup(x)));
	return (z,0:𝔹);
}
def main()⇒foo(1);
