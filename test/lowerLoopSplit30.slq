// args: --remove-loops --run

def foo(const a:𝔹,n:!ℕ){
	c:=dup(a);
	d:=0:𝔹;
	for i in [0..n){
		j:=0:!ℕ;
		while j<i{
			c⊕=1;
			if c { d:=H(d); }
			j+=1;
		}
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a,4);
	return (a,r);
}
