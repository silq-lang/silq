
def cond[τ:type,σ:type](c:!𝔹,a:τ,b:σ): if c then let σ:=τ in σ else σ; /// TODO
def leak(){ /// TODO error
	c:=measure(H(0));
	a:=[4];
	b:=[1,2,3];
	return cond(c,a,b);
}
def main(){
	a:=[4];
	b:=[1,2,3];
	(x,y):=(cond(true,a,b),cond(false,[2],[7,8]));
	return (x,y);
}
