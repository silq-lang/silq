// args: --run

def main(){
	x:=H(0);
	with{
		y:=H(0);
		def g(i:!ℕ){
			if i=0{ return; }
			if x{ if y{ phase(π); } }
			return g(i sub 1);
		}
	}do{
		g(2);
		x:=H(H(x));
	}
	assert(measure(H(x))=0);
}
