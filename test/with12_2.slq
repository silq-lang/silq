
def len(const x: 𝔹[]) qfree: !ℕ {
	return x.length;
}

def main(){
	x := ((0,),);
	y := vector(2, 0);

	with {
		i := len(x[0]);
		j := 0;
		(yleft: _^(i+j)) ~ (yi,) ~ (yright) := y; /// TODO
	} do {
		yi := H(yi);
	}
	return (x, y);
}
