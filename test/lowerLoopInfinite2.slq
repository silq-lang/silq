// args: --remove-loops --run

def f(const a:𝔹){
	c:=dup(a);
	d:=0:𝔹;
	k:=0:!ℕ;
	while true {
		c⊕=1;
		if c { d:=H(d); }
		k+=1;
		if k==3 { return d; }
	}
	forget(c);
	return X(d);
}
def main(){
	a:=H(0:𝔹);
	r:=f(a);
	return (a,r);
}
