
def foo(const x:𝔹,y:!𝔹,z:!𝔹)wild{
	if x{
		if y{
			return [0,1];
		}
	}else{
		if z{
			return [1,0];
		}
	}
	return [y,z];
}

def main(){
	for i in 0..2{
		for j in 0..2{
			for k in 0..2{
				measure(foo(i≠0,j≠0,k≠0)); // TODO: check with assert
			}
		}
	}
}
