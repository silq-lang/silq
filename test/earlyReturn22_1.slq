
def foo(const x:𝔹,y:!𝔹,z:!𝔹)wild{
	if x{
		if y{
			return [0,1];
		}
	}else{
		if z{
			return [1,0];
		}
	}
	return [y,z];
}

def main(){
	for i in 0..2{
		for j in 0..2{
			for k in 0..2{
				r:=measure(foo(i≠0,j≠0,k≠0));
				if i≠0&&j≠0{ assert(r=[0,1]); }
				if i=0&&k≠0{ assert(r=[1,0]); }
				if i=0&&k=0{ assert(r=[j≠0,0]); }
			}
		}
	}
}
