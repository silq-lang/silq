def main(){
    a := array(3);
    k := UniformInt(1,3);
    for i in [0..3) { a[i] = Bernoulli((i+1)/3)/k; }
    x := Categorical(a);
    return x; // expected: 31·δ[-2+x]·⅟162+7·δ[x]·⅟162+8·δ[-1+x]·⅟81
}