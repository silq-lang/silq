// args: --remove-loops --run

def foo(const a:𝔹,const b:𝔹){
	c:=dup(a);
	e:=dup(b);
	d:=0:𝔹;
	k:=0:!ℕ;
	for i in [0..5){
		k+=i;
		c⊕=1;
		if k%2==0 {
			e⊕=c;
			if e { d:=H(d); }
		}
	}
	return (d,k);
}
def main(){
	a:=H(0:𝔹);
	b:=H(0:𝔹);
	r:=foo(a,b);
	return (a,b,r);
}
