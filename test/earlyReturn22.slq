// args: --run

def foo(const x:𝔹,const y:𝔹)wild{
	if x {
		return 1;
	}else if y {
		return 1;
	}
	return 0;
}

def bar(const x:𝔹,const y:𝔹)wild{
	if x {
		if y {
			return 1;
		}
	}else{
		return 1;
	}
	return 0;
}

def main(){
	for i in 0..2{
		for j in 0..2{
			assert(measure(foo(i≠0,j≠0))=(i≠0|j≠0));
			assert(measure(bar(i≠0,j≠0))=(i=0|j≠0));
		}
	}
}
