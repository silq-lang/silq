// args: --remove-loops --run

def foo(const a:𝔹,const b:𝔹){
	c:=dup(a);
	d:=0:𝔹;
	for i in [0..3){
		if a {
			if b {
				c⊕=1;
				if c { d:=H(d); }
			}
		}else{
			c⊕=b;
			d:=X(d);
			if c { d:=H(d); }
		}
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	b:=H(0:𝔹);
	r:=foo(a,b);
	return (a,b,r);
}
