// args: --remove-loops --run

def foo(const a:𝔹){
	c:=dup(a);
	d:=0:𝔹;
	k:=0:!ℕ;
	for i in [0..3){
		while k<2*i{
			k+=1;
			c⊕=1;
			if c { d:=H(d); }
		}
	}
	return (d,k);
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a);
	return (a,r);
}
