// args: --run

def main(){
	def r()⇒measure(H(0:𝔹));
	x₀:=if r() then (0:!𝔹,0:𝔹) else vector(2,0:𝔹);
	x₁:=if r() then vector(2,0:𝔹) else (0:!𝔹,0:𝔹);
	y₀:=if r() then array(2,0:𝔹) else (0:!𝔹,0:𝔹);
	y₁:=if r() then (0:!𝔹,0:𝔹) else array(2,0:𝔹);
	z₀:=if r() then vector(2,0:𝔹) else array(2,0:𝔹);
	z₁:=if r() then array(2,0:𝔹) else vector(2,0:𝔹);
	return (x₀,x₁,y₀,y₁,z₀,z₁);
}
