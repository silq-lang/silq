def main(){
	def foo[a](x:a){ // TODO
		def id(x:a)mfree⇒(dup(x),x);
		return reverse(id)(dup(x),x); // TODO: add type of types without classical components
	}
	return (foo([0,1]),foo(H(0:𝔹)));
}
