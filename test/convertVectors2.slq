// args: --run

n:=3;
k:=2;

def main(){
	w₁:=(1,0,1,0):!𝔹^(2^k);
	w₂:=(1:!𝔹,2,3,4);
	t₁:=w₁ as !uint[n]^(2^k);
	t₂:=w₂ as !uint[n]^(2^k);
	t₃:=w₁ as 𝔹×!𝔹×uint[n]×!𝔹;
	t₄:=w₁ as !𝔹×!ℕ×!ℕ×!ℕ;
	return (t₁,t₂,t₃);
}
