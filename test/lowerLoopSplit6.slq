// args: --remove-loops --run

def foo(const a:𝔹){
	c:=dup(a);
	u:=dup(a);
	v:=0:𝔹;
	for i in 0..4{
		t:=dup(c);
		u⊕=t;
		v⊕=t;
		v:=H(v);
		c⊕=1;
	}
	return v;
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a);
	return (a,r);
}
