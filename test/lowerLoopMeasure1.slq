// args: --remove-loops --run

def foo(const a:𝔹){
	c:=dup(a);
	d:=0:𝔹;
	k:=0:!ℕ;
	while k<3{
		b:=measure(H(0:𝔹));
		if b {
			c⊕=1;
			k+=1;
		}
		if c { d:=H(d); }
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a);
	return (a,r);
}
