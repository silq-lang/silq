// args: --run

def foo(const x:𝔹,y:!𝔹,z:!𝔹,w:!𝔹,t:!𝔹)wild{
	if x{
		if y{
			return 1;
		}
		if w{
			return 1;
		}
	}else{
		if z{
			return 1;
		}
		if t{
			return 1;
		}
	}
	assert(!y&&!w||!z&&!t);
	return 0;
}

def main(){
	for y in 0..2{
		for z in 0..2{
			for w in 0..2{
				for t in 0..2{
					for k in 0..10{
						x:=H(0);
						assert(measure(foo(x,y≠0,z≠0,w≠0,t≠0))||y=0&&w=0||z=0&&t=0);
						measure(x);
					}
				}
			}
		}
	}
}
