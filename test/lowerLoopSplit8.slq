// args: --remove-loops --run

def foo(const a:𝔹[],n:!ℕ){
	vs:=vector(n,0:𝔹): 𝔹[];
	q:=0:𝔹;
	for k in [0..n){
		vs[k]⊕=a[k];
		if k>0 { if vs[k-1] { q:=H(q); } }
	}
	return q;
}
def main(){
	a:=[H(0:𝔹),1:𝔹,0:𝔹]: 𝔹[];
	r:=foo(a,3);
	return (a,r);
}
