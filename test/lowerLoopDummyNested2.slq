// args: --remove-loops --run

def main(){
	q := H(0:𝔹);
	v := dup(q);
	w := dup(q);
	for l in [0..2){
		for j in [0..2){
			for i in [0..2){
				forget(v);
				v := dup(q);
				w := X(w);
			}
		}
	}
	return (q, v, w);
}
