def foo(a:𝔹,const cond:𝔹,const cond2:𝔹){
	if cond2 {
	x:=!a;
	y:=!x;
	if cond{
		x:=H(x);
		// forget(y=a);
	}else{
		a:=H(a); // other test case: comment out this block,
		         // should work with y depending on a
		// forget(y=!x)
		//z:=x;
	}
	//(x):=factored(cond,x);
	__show(__query("dep",y)); // wrong
	forget(y); // TODO: error
	return (a,x);
	}else{
		return (a,0:𝔹);
	}
}
