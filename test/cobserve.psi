
def main(){
    x := uniform(0,1);
    y := uniform(x,2*x);
    cobserve(y,2/3);
    return x; // expected: [-1+x≤0]·[-2·⅟3+x≤0]·[-x+⅟3≤0]·[-x≤0]·[x≠0]·⅟log(2)·⅟x
    // TODO: get rid of spurious brackets
}
