// skipped

def main(){
    x₁ := gauss(0,1);
    x₂ := gauss(0,1);
    x₃ := gauss(0,1);
    x₄ := gauss(0,1);
    x₅ := gauss(0,1);
    x₆ := gauss(0,1);
    x₇ := x₁+x₂;
    x₈ := x₃+x₄;
    x₉ := x₆+x₇;
    return x₁>1||x₂>1||x₃>1||x₄>1||x₅>1||x₆>1||x₇>1||x₈>1||x₉>1;
}
