
def main(){
	x₁ := uniform(-1000,1000);
	x₂ := uniform(-1000,1000);
	observe((1 <= x₁) & (x₁ <= 3) & (4 <= x₂) & (x₂ <= 8));
	y₁ := 10*x₁+20*x₂;
	y₂ := 30*x₁+40*x₂;
	return (y₁,y₂); // expected: 1/1600·[-160+-y₂+3·y₁≤0]·[-2·y₁+-30+y₂≤0]·[-3·y₁+80+y₂≤0]·[-y₂+10+2·y₁≤0]
}
