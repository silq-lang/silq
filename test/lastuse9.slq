
def main(){
	x:=0:𝔹;
	y:=0:𝔹;
	if x{ x=y; }
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	if x{ k=y; } /// error
	return y;
}
