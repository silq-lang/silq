// args: --run

def main(){
	x:=1:ℤstar[53];
	t:=H(0);
	if t{ x=2 coerce ℤstar[53]; }
	b:=3 coerce !ℤstar[53];
	x·=b^0·b^1;
	x·=b^2;
	x·=b^-1;
	assert(measure(if t then x==18 else x==9));
	return (x,t);
}
