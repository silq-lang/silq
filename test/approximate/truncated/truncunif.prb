//skipped

def df() {
    return Uniform(-0.5,0.5);
}


def main(){

    assert(Bernoulli(0.999));
    assert(Bernoulli(0.999));
    assert(Bernoulli(0.999));

    x:=df();
    y:=df();
    z:=df();

    v := 0.25*(x+y+z);
    
    return v;

}