// skipped


def main() {

    assert(Bernoulli(0.999));
    assert(Bernoulli(0.999));
    assert(Bernoulli(0.999));


    X := Laplace(0, 0.2); observe(-0.5 <= X && X <= 0.5);
    Y := Laplace(0, 0.2); observe(-0.5 <= Y && Y <= 0.5);
    Z := Laplace(0, 0.2); observe(-0.5 <= Z && Z <= 0.5);

    return 0.25*(X+Y+Z);
//      return (X,Y);


}