

def f(dif) {
    	xi := Uniform(10,20);
    	xsi := xi + Laplace(0,1);
 	return dif + (xi - xsi);
}

def main() {

    n := 5;

//    x := Laplace(0,2);
//    return x;

/*
    x := array(n);
    xs := array(n);

    for i in [0..n) {
    	x[i] = Uniform(10,20);
    	xs[i] = x[i] + Laplace(0,1);
    }

    dif := 0;
    for i in [0..n) {
    	  dif = dif + (x[i] - xs[i]);
    }
    dif = dif / n;
    return dif;

*/

    dif := 0;
    for i in [0..n) {
       	xi := Uniform(10,20);
    	xsi := xi + Laplace(0,1);
 	dif = dif + (xi - xsi);
	
//    	dif = f(dif);
    }

    dif = dif / n;
    return dif; // expected: -1875·[-dif≠0]·[dif≤0]·dif²·e^(5·dif)·⅟128+-1875·[-dif≤0]·dif²·⅟256·⅟e^(5·dif)+-243475·[-dif≤0]·⅟1536·⅟e^(5·dif)+-29125·[dif=0]·⅟384+-3125·[dif≤0]·dif³·e^(5·dif)·⅟384+-359975·[-dif≠0]·[dif≤0]·e^(5·dif)·⅟1536+-875·[dif≤0]·dif·e^(5·dif)·⅟256+1875·[-dif≠0]·[-dif≤0]·dif²·⅟128·⅟e^(5·dif)+244525·[-dif≠0]·[-dif≤0]·⅟1536·⅟e^(5·dif)+3125·[-dif≤0]·dif³·⅟384·⅟e^(5·dif)+3125·[-dif≤0]·dif⁴·⅟768·⅟e^(5·dif)+3125·[dif≤0]·dif⁴·e^(5·dif)·⅟768+361025·[dif≤0]·e^(5·dif)·⅟1536+5625·[dif≤0]·dif²·e^(5·dif)·⅟256+875·[-dif≤0]·dif·⅟256·⅟e^(5·dif)

}
