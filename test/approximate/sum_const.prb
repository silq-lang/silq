

def f(dif) {
    	xi := Uniform(10,20);
    	xsi := xi + Laplace(0,1);
 	return dif + (xi - xsi);
}

def main() {

    n := 5;

//    x := Laplace(0,2);
//    return x;

/*
    x := array(n);
    xs := array(n);

    for i in [0..n) {
    	x[i] = Uniform(10,20);
    	xs[i] = x[i] + Laplace(0,1);
    }

    dif := 0;
    for i in [0..n) {
    	  dif = dif + (x[i] - xs[i]);
    }
    dif = dif / n;
    return dif;

*/

    dif := 0;
    for i in [0..n) {
       	xi := Uniform(10,20);
    	xsi := xi + Laplace(0,1);
 	dif = dif + (xi - xsi);
	
//    	dif = f(dif);
    }

    dif = dif / n;
    return dif; // expected: (-125·[dif≤0]·dif·⅟64+-625·[dif≤0]·dif³·⅟96+175·[-dif≤0]·[dif≠0]·⅟512·⅟e^(10·dif)+175·[dif=0]·⅟512+175·[dif≠0]·[dif≤0]·⅟512+3125·[dif≤0]·dif⁴·⅟768+375·[-dif≤0]·[dif≠0]·dif·⅟256·⅟e^(10·dif)+625·[-dif≤0]·[dif≠0]·dif²·⅟256·⅟e^(10·dif)+625·[-dif≤0]·[dif≠0]·dif³·⅟384·⅟e^(10·dif)+625·[dif≤0]·dif²·⅟128)·e^(5·dif)+(-375·[dif≠0]·[dif≤0]·dif·e^(10·dif)·⅟256+-625·[dif≠0]·[dif≤0]·dif³·e^(10·dif)·⅟384+125·[-dif≤0]·dif·⅟64+175·[-dif≤0]·[dif≠0]·⅟512+175·[dif=0]·⅟512+175·[dif≠0]·[dif≤0]·e^(10·dif)·⅟512+3125·[-dif≤0]·dif⁴·⅟768+625·[-dif≤0]·dif²·⅟128+625·[-dif≤0]·dif³·⅟96+625·[dif≠0]·[dif≤0]·dif²·e^(10·dif)·⅟256)·⅟e^(5·dif)

}