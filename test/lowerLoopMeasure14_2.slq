// args: --remove-loops --run

def flip(b:!𝔹,x:𝔹)qfree{
	if b { x:=X(x); }
	return x;
}
def foo(const a:𝔹){
	c:=dup(a);
	d:=0:𝔹;
	for i in [0..3){
		c:=flip(measure(H(dup(c))),c); /// TODO
		if c { d:=H(d); }
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a);
	return (a,r);
}
