// args: --run

def approx(a:!ℝ,b:!ℝ)⇒abs(a-b)≤1.0e-10;

def main() {
	assert(floor(3.5) == 3);
	assert(floor(-3.5) == -4);
	assert(sqrt(4) == 2);

	assert(approx(sqrt(5), 2.23606797749979));
	assert(approx(cos(1), 0.5403023058681398));
}
