
def maink(){
    x := Uniform(0,1);
    y := Uniform(0,1);
    k := Uniform(0,1);
    a := x*y+k;
    return a; // expected: ((-[-a≠0]·a·log(a)+a)·[-1+a≤0]·[-a≤0]+-[-2+a≤0]·[-a+1≠0]·[-a+1≤0]·a+-[-2+a≤0]·[-a+1≠0]·[-a+1≤0]·log(-1+a)+2·[-2+a≤0]·[-a+1≠0]·[-a+1≤0]+[-2+a≤0]·[-a+1≠0]·[-a+1≤0]·a·log(-1+a))·`q()
    // TODO: normalization: log(ax+b)*poly(x)
}
