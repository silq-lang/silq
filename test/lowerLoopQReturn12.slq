// args: --remove-loops --run

def f(const a:𝔹,const b:𝔹){
	c:=dup(a);
	d:=0:𝔹;
	for i in [0..3){
		c⊕=1;
		if c { d:=H(d); }
		if b { return H(d); }
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	b:=H(0:𝔹);
	r:=f(a,b);
	return (a,b,r);
}
