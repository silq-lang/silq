// args: --run

def myH(x:𝔹)mfree{
	x:=H(x);
	return x;
}

def main(){
	xs:=(H(0),H(1));
	with {
		t:=myH(xs[0]);
		u:=myH(xs[1]);
	} do {
		(t,u):=(u,t);
	}
	xs[0]:=H(xs[0]);
	xs[1]:=H(xs[1]);
	assert(measure(xs=(1,0)));
	return xs;
}
