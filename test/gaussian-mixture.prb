
def main(){
    tau := 1;
    sigma := 1;
    mu1 := Gauss(0,tau^2);
    mu2 := Gauss(0,tau^2);
    
    x1 := if Bernoulli(1/3) { Gauss(mu1, sigma) } else { Gauss(mu2, sigma) };
    x2 := if Bernoulli(1/3) { Gauss(mu1, sigma) } else { Gauss(mu2, sigma) };
    return (x1,x2); // expected: TODO: this should be simplified!
}