// args: --remove-loops --run

def foo(const a:𝔹,const b:𝔹,n:!ℕ){
	c:=dup(a);
	e:=dup(b);
	d:=0:𝔹;
	for i in [0..n){
		if i+1<n {
			if a { c⊕=1; }
			d:=H(d);
		}
		if c {
			e⊕=1;
			d:=H(d);
		}
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	b:=H(0:𝔹);
	r:=foo(a,b,4);
	return (a,b,r);
}
