// skipped
// TODO
def T(a,b){
    return Uniform(0,1)+Uniform(0,10); // TODO: reproduce their triangular distribution precisely.
}

def main(){ // tracking
    data := readCSV("data/tracking/query1.csv");
    dd := Uniform(0,10);
    for i in [0..data.length){
        which := Uniform(0,1);
	tracking := if which < 0.05 { Uniform(0,10) } else { if which < 0.9 { Gauss(dd,2) } else { T(10,1) }};
	//tracking := Gauss(dd,2);
	cobserve(tracking,data[i]);
    }
    return dd;
}
