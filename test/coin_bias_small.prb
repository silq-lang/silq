def test(){
    p := Uniform(0,1);
    x1 := Bernoulli(p);
    observe(x1==1);
    x2 := Bernoulli(p);
    observe(x2==1);
    x3 := Bernoulli(p);
    observe(x3==1);
    x4 := Bernoulli(p);
    observe(x4==1);
    x5 := Bernoulli(p);
    observe(x5==0);
    return p; // expected: (-30·p+30)·[-1+p≤0]·[-p≤0]·p⁴
}