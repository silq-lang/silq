// args: --run

def main(){
	x:=H(0:𝔹);
	y:=H(0:𝔹);
	a:=vector(3,0:𝔹);
	(if x then /// TODO
		if y then (a[0],a[1]) else (H(a[1]),a[0])
	else
		if y then (a[1],a[0]) else (H(a[0]),a[1])
	) := (a[0],H(a[1]));
	return (x,y,a);
}
