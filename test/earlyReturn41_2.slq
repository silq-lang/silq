// args: --run

def foo(x:!𝔹,y:!𝔹,z:𝔹,w:𝔹)lifted{
	if x{
		if y{ return 1; }
		if z{ return 1; }
	}else{
		if w{ return 1; }
	}
	return 0;
}

def main(){
	for i in 0..2{
		for j in 0..2{
			for k in 0..2{
				for l in 0..2{
					assert(measure(foo(i≠0,j≠0,k≠0,l≠0))=(if i≠0 then j≠0||k≠0 else l≠0));
				}
			}
		}
	}
}
