// args: --run

def myH(x:𝔹)mfree{
	x:=H(x);
	return x;
}

def main(){
	xs:=(H(0),H(0));
	with {
		t:=myH(xs[0]);
		u:=myH(xs[1]);
		(t,u):=(u,t);
	} do {
		u⊕=1;
	}
	xs[0]:=H(xs[0]);
	xs[1]:=H(xs[1]);
	assert(measure(xs=(1,0)));
	return xs;
}
