// args: --remove-loops --run

def foo(const a:𝔹){
	x:=(dup(a),0:𝔹);
	d:=0:𝔹;
	for i in [0..3){
		if x[0] {
			x[1]:=X(x[1]);
			d:=H(d);
		}
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a);
	return (a,r);
}
