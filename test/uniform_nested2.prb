// skipped
// TODO: fix

def main(){
    a := Uniform(0,1);
    b := Uniform(0,a);
    c := Uniform(0,b);
    d := Uniform(0,c);
    return d; // expected: -[-1+d≤0]·[-d≤0]·log(d)³·⅟6
    // TODO: confirm that this is correct
}