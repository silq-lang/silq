// --run

def foo[n:!ℕ](i:!ℕ,x:((!𝔹 × !𝔹 × !ℚ)^1)^n){
	(x[1][0][i],x[0])=(2,[[0,1,2],[0,0,0]]);
	return x;
}

def bar[n:!ℕ](i:!ℕ,x:((!𝔹 × !𝔹 × !ℚ)^1)^n){
	(x[1][0][i],x[0])=(2,[[0,1,2],[0,0,0,0]]);
	return x;
}

def main(){
	x:=[[(0,1,2/3)],[(0,1,2/3)]];
	y:=x;
	i:=0;
	__show(typeof(x));
	x=foo(0,x);
	y=bar(0,y);
	__show(typeof(x));
	__show(typeof(y));
	return (x,y);
}
