
def main(){
    x₁ := Gauss(0,1);
    x₂ := Gauss(0,1);
    x₃ := Gauss(0,1);
    x₄ := Gauss(0,1);
    x₅ := Gauss(0,1);
    x₆ := Gauss(0,1);
    x₇ := x₁+x₂;
    x₈ := x₃+x₄;
    x₉ := x₆+x₇;
    return x₁+2*x₂+3*x₃+4*x₄+5*x₅+6*x₆+7*x₇+8*x₈+9*x₉; // expected: ⅟2·⅟e^(r₁²·⅟2256)·⅟√5̅6̅4̅·⅟√π̅
}
