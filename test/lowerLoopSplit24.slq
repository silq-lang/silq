// args: --remove-loops --run

def cx(x:𝔹,const c:𝔹)qfree:𝔹{ if c { x:=X(x); } return x; }
def foo(const a:𝔹){
	c:=dup(a);
	d:=0:𝔹;
	for i in [0..3){
		c⊕=1;
		d:=H(d);
		d:=reverse(cx)(d,c);
	}
	return d;
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a);
	return (a,r);
}
