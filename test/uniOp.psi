
def main(){
	x := 0;
	x = 1 ≤ 1;
	assert(x);
	x = 1 = 1;
	assert(x);
	x ← 1 = 1;
	(a,b) := (1,2);
	(a,b) ← (b,a);
	assert(a=2 ∧ b=1);
	assert(¬(⊥ ∨ ⊥));
	assert(⊤ ∨ ⊥);
	a ∨= 1;
	a ∧= 7;
	a ∨← 1;
	a ∧← 7;
	a +← 3;
	return 2·a; // expected: δ(12)[r]
}
