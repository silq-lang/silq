// args: --run

def foo(k:!ℕ,m:!ℕ) ⇒ vector(let { n:=k+m; t:=n; } in t, false); /// TODO
def chain(k:!ℕ) ⇒ vector(let { a:=k; b:=a+a; } in b, true); /// TODO
def main(){
	n:=3;
	x:=foo(n,n);
	b:=3;
	y:=chain(b);
	forget(x=vector(6,false));
	forget(y=vector(6,true));
	return (n,b);
}
