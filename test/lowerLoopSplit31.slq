// args: --remove-loops --run

def foo(const a:𝔹){
	c:=dup(a);
	d:=0:𝔹;
	k:=0:!ℕ;
	while k<5{
		k+=2;
		if k%3==0 { c⊕=1; }
		if c { d:=H(d); }
	}
	return (d,k);
}
def main(){
	a:=H(0:𝔹);
	r:=foo(a);
	return (a,r);
}
