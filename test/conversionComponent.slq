// args: --run

def f() mfree: B^(1 sub 1) {
	return ();
}

def g() qfree: B^(1 sub 1) {
	return ();
}

def main() {
	x1 := f() as B^0;
	x2 := measure(f());
	x3 := measure(f() as B^0);
	x4 := measure[B^0](f());
	y1 := g() as B^0;
	y2 := measure(g());
	y3 := measure(g() as B^0);
	y4 := measure[B^0](g());
	return x1;
}
