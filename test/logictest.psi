
def main(){
    r := false;
    r ||= flip(1/2);
    s := true;
    s &&= flip(1/2);
    t := true;
    t ||= flip(1/2);
    u := false;
    u &&= flip(1/2);

    v := flip(1/2) && flip(1/2);
    w := flip(1/2) || flip(1/2);

    x := r&&r;
    y := s||s;

    return (r,s,t,u,v,w,x,y); // expected: (1/2·δ(0)[-r+1]+1/2·δ(0)[r])·(1/2·δ(0)[-s+1]+1/2·δ(0)[s])·(1/4·δ(0)[-v+1]+3/4·δ(0)[v])·(1/4·δ(0)[w]+3/4·δ(0)[-w+1])·([r=0]·δ(0)[x]+[r≠0]·δ(0)[-x+1])·([s=0]·δ(0)[y]+[s≠0]·δ(0)[-y+1])·δ(0)[-t+1]·δ(0)[u]
    // TODO: nicer simplification?
}
